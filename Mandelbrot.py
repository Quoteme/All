#!/usr/bin/python
import pygame as pygame
import sympy as sp
import numpy as np

#def checkMandelbrot(x):


def Mandelbrot(screenData, pos, button, Mode, Run):
    screen ,screen_width, screen_height, Clock= screenData

    return Mode, Run, pos, button
